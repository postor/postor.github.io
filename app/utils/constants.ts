/**
 * Internal storage root path constant
 * This should never be exposed to users in UI or URL parameters
 */
export const STORAGE_ROOT = 'my-books'

/**
 * Hidden file prefix for internal files
 */
export const HIDDEN_PREFIX = '_'
